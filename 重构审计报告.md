# 第二课堂系统 · 代码审计与重构路线图

> 生成日期: 2026-06-01
> 范围: backend (FastAPI) + frontend (Vue3 管理端)
> 方式: **渐进式分模块重构** —— 不推倒重来,保留现有可运行系统,逐模块替换,每步可上线可回退

---

## 0. 总原则(先读这个)

1. **不重写,只重构。** 现系统在生产、有真实数据(5000+ 活动 / 3 万学生)、踩过的坑(滑块验证码/分页边界/QQ提取/状态映射)都埋在代码里。推倒重来会把坑再踩一遍。
2. **每个阶段独立可上线、可回退。** 一个模块改完、验证、提交,再动下一个。任何一步出问题都能单独回滚,不影响其它。
3. **先安全、再结构、后洁癖。** 顺序按"会不会出事"排,不是按"代码好不好看"排。
4. **务实优先级。** 校内 + 个人项目的规模下,有些"教科书异味"(N+1 查询、单实例调度)其实不痛,不强行 P0。下面优先级是按这个项目的真实情况定的。

---

## 1. 现状分级

### 🟢 写得还行,别动
- **部署层**: docker-compose / frp 隧道 / Caddy+HTTPS / systemd —— 稳定,已验证
- **小程序**: `微信小程序/` 刚重写,结构干净
- **models / schemas 的 ORM 定义**: 字段完整,大部分和 DB 一致
- **前端 api/request.js**: axios 拦截器、401/403/404/500 统一处理 —— 完整
- **wechat_service token 缓存**: 线程安全 + 自动刷新,架构扎实
- **管理端 JWT 鉴权**: 26 个管理端点全覆盖,登录/校验规范

### 🟡 能用但乱,值得重构
- `services/crawler_service.py` (658行): `_save_xxx_to_db` 4 个方法把数据访问混进业务
- `crawlers/` 五个爬虫各自为政,分页/去重/取数 JS/翻页等待重复 3-4 处
- `api/crawler.py` (900行): 8 个后台任务端点同一套样板重复 ~20 行
- `frontend CrawlerManagement.vue` (**1536行**): 6 个独立功能、40+ 状态、42 方法、3 套轮询 —— **头号屎山**
- 状态/颜色映射散落前端 3+ 处(已多次因此踩坑,如 9 状态那次)
- 数据库三套 schema 定义(ORM / database_schema.sql / 历史 SQL),会漂移
- print 而非 logging,错误只 `str(e)` 丢栈

### 🔴 真空白 / 真隐患(优先)
- **安全**: `config.py` 硬编码弱密码/邮箱授权码/JWT密钥/爬虫凭证; `public` + `wechat` 共 6 端点完全裸奔; admin 默认密码 admin123 无强制改
- **调度**: 自研 threading 调度器,进程重启任务全丢
- 无数据库迁移工具(alembic),schema 改动只能手工维护

---

## 2. 分模块问题清单

### 2.1 后端 · 安全(🔴 最高)
| 问题 | 位置 | 风险 |
|---|---|---|
| 弱密码/密钥硬编码为默认值 | `config.py` DB_PASSWORD/SECRET_KEY/EMAIL_AUTH_CODE/KETANG_PASSWORD | .env 缺失时静默用弱默认值;代码泄露=全线失守 |
| public API 无鉴权 | `api/public.py` 3 端点 | 设计如此(给小程序),但应加限流/防刷 |
| wechat 代理无鉴权 | `api/wechat.py` 3 端点 | 任何人可调 `/send` 群发订阅消息,薅配额/骚扰 |
| admin 默认密码 | `init_db.py` admin/admin123 | 无首次强制改密 |
| 无限流 | login / wechat / schedule | 可暴力破解 / 刷配额 |
| CORS 过宽 + DEBUG=True 默认 | `main.py` / `config.py` | 生产泄栈 |

### 2.2 后端 · 数据访问混业务(🟡)
- `crawler_service.py` 的 `_save_activities_to_db` / `_save_activity_details_to_db` / `_save_participants_to_db` / `_save_students_to_db` —— SQLAlchemy query/add/commit 直接嵌在业务方法里。难测、难复用。
- 字段映射有拼写坑: `class_name=detail.get("calssName")`(原数据拼写)。
- 错误处理 `except Exception as e: self._log(str(e))` —— 丢栈,排错痛。

### 2.3 后端 · 爬虫层重复(🟡,最近一直在改这块)
- 5 个爬虫无公共基类。以下逻辑各写一遍:
  - 翻页+等待数据变化+去重: `activity_crawler` / `participant_crawler` / `student_crawler` 各 1 套
  - 读 Vue `$data` 的 JS 脚本: 4 处,逻辑 80% 相同
  - page_size 设置: 4 处不同实现
  - driver 生命周期: 各自 `if __name__` 里 quit,无统一管理
- 硬编码 XPath/CSS/sleep 散落(`time.sleep(3)` 5 处、`sleep(2)` 6 处)
- `student_crawler.py` 789 行最大(含源站 API + AES,这部分是真本事,保留)

### 2.4 后端 · 调度(🔴 结构 / 🟡 紧迫度)
- `scheduler_service.py` 自研 `Thread + Event + sleep(1) 轮询`。无持久化,重启丢任务。单实例下能跑,但脆。

### 2.5 后端 · API 样板重复(🟡)
- `crawler.py` 8 个后台任务端点 (`crawl-activities` / `details` / `students` / `participants` / `full` / 3 个 script) 重复同一套:
  `create_task → background_tasks.add_task(run_crawler) → try/状态更新/except/finally stop_driver`。
- Pydantic schema 内联在各 api 文件; `public.py` 直接返回 dict 无类型。

### 2.6 后端 · 数据库 schema 漂移(🟡)
- `activities` / `activity_details` 两张表**只在 ORM 有,database_schema.sql 缺失**。
- crawler_logs 有 3 个历史 SQL 版本,ORM 缺 task_id UNIQUE 约束。
- 无 alembic。`init_db.py` 用 `create_all`,不跑 SQL 初始化。

### 2.7 后端 · 死代码 / 重复脚本(🟢 清理)
- `init_database.py` 已被 `init_db.py` 取代 —— 死代码
- `create_crawler_logs_table.py` / `add_qq_groups_field.py` / `check_students_table.py` / `update_password.py` —— 一次性已执行,可归档
- `main_crawler.py`(完整流程+发邮件) vs `full_crawl.py`(只爬+存库) —— 功能重叠,需明确分工或合并

### 2.8 前端 · 巨型组件(🔴 维护成本)
`CrawlerManagement.vue` 1536 行 = template 370 + script 1009 + style 155,承担 6 件事:
爬虫卡片 / 定时配置 / 任务历史表 / 日志对话框 / 活动选择对话框 / 通知脚本面板。
- 40+ 状态、42 方法、3 套独立轮询定时器(poll/schedule/log)
- `startCrawler` 与 `startScript` 逻辑 90% 重复
- 混入原生 `fetch`(本该走 api 层)

### 2.9 前端 · 散落与冗余(🟡)
- 状态/颜色映射在 CrawlerManagement / ActivityList / Dashboard / NotificationLog 各写一遍
- 分页样板 4 处重复
- `stores/auth.js` 与 `stores/user.js` 职责重叠

---

## 3. 目标架构

### 后端
```
backend/app/
  core/                 # 基础设施(新)
    config.py           # 强类型,无默认密码,缺失即启动失败
    logging.py          # 统一 logging(替换 print)
    database.py
    security.py         # JWT / 密码 / 服务间 token
  models/               # ORM(保留)
  schemas/              # Pydantic 独立出来(从 api 抽离)
  repositories/         # 数据访问层(新): activity / student / participant / notification
  crawlers/
    base.py             # BaseCrawler: driver/分页/去重/取Vue数据 公共逻辑
    selectors.py        # XPath/CSS/timing 集中
    login.py activity.py detail.py participant.py student.py
  services/             # 业务编排(瘦身,只编排不碰 DB)
  tasks/                # 调度(APScheduler+jobstore) + 后台任务工厂(消重)
  api/
    deps.py
    admin/  public/  wechat/   # 按受众分组
  main.py
alembic/                # 迁移(新)
```

### 前端
```
frontend/src/
  api/                  # 保留,补统一响应模型
  stores/
    auth.js             # 合并 user.js
    crawler.js          # 新: 爬虫/脚本/轮询状态
  composables/
    usePagination.js  usePolling.js   # 抽公共
  constants/enums.js    # 状态/颜色映射集中(单一来源)
  components/crawler/
    CrawlerCard.vue  ScriptCard.vue
    ActivityPickerDialog.vue  TaskLogDialog.vue  TaskHistoryTable.vue
  views/CrawlerManagement.vue   # 瘦身到 ~300 行,只负责组装
```

---

## 4. 迁移路线图(分阶段,每阶段可独立上线/回退)

| 阶段 | 名称 | 内容 | 痛点级别 | 风险 | 验证方式 |
|---|---|---|---|---|---|
| **0** | 清死代码 + alembic 基线 | 删/归档一次性脚本; 引入 alembic,以当前 DB 为基线(不改行为) | 🟢 | 极低 | 容器正常起、表无变化 |
| **1** | core 基础设施 | config 去硬编码(缺失即报错)+ 统一 logging + security.py 抽出 | 🔴 安全地基 | 低 | 健康检查、登录、爬虫各跑一次 |
| **2** | 鉴权闭环 | public/wechat 加服务间 token + 限流; admin 首次强制改密 | 🔴 安全 | 中 | 小程序/管理端全链路回归 |
| **3** | 数据层 repositories | 把 `_save_xxx_to_db` 抽到 repository; 统一 schema 进 alembic | 🟡 | 中 | 跑一次综合爬取,数据入库对比 |
| **4** | 爬虫层 BaseCrawler | 抽 driver/分页/去重/取数公共基类 + selectors 集中 | 🟡(高频改动区) | 中 | 综合爬取数量与重构前一致 |
| **5** | API 瘦身 | schema 独立 + 后台任务工厂消 8 处重复样板 | 🟡 | 低 | 各爬虫端点回归 |
| **6** | 调度升级 | 自研 threading → APScheduler + jobstore(持久化) | 🟡 | 中 | 定时任务重启后仍在 |
| **7** | 前端拆分 | CrawlerManagement 拆组件 + crawler store + composable + enums 集中 | 🔴 维护成本 | 中 | 管理端逐功能回归 |

> 建议执行顺序: **0 → 1 → 2**(地基+安全先稳)→ 之后 3/4/5/7 可按你的优先级穿插,6 最不急。

---

## 5. 一句话结论

骨架合格(分层、Docker、鉴权框架都在),不是该推倒的烂摊子。真正的问题集中在三块:**安全空白**(上线前必补)、**爬虫层与前端巨型组件的重复**(改一处动五处的维护税)、**数据库/调度的基础设施欠账**。按上面 0→1→2 打好地基,剩下逐模块清,每步都能安全上线。
