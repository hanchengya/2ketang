# 第二课堂活动通知系统 - 开发进度

## 最后更新: 2026-05-12

---

## 一、整体进度概览

| 模块 | 完成度 | 状态 |
|------|--------|------|
| 数据爬取模块 | 100% | 已完成 |
| 数据库设计 | 100% | 已完成 |
| 后端API | 95% | 基本完成 |
| 业务服务层 | 100% | 已完成 |
| 前端开发 | 85% | 开发中 |
| 定时任务 | 100% | 已完成 |
| 系统优化 | 0% | 待开发 |

---

## 二、详细开发记录

### 2026-05-12 更新

#### 项目启动验证

**完成内容:**
- 已启动后端服务 `http://127.0.0.1:8000/`。
- 已启动前端服务 `http://127.0.0.1:5173/`。
- 已验证后端健康检查 `http://127.0.0.1:8000/health` 返回 `200 OK`。
- 已验证前端首页返回 `200 OK`。
- 已验证 `admin/admin123` 后端直连登录返回 `200 OK`。
- 已验证 `admin/admin123` 前端代理登录返回 `200 OK`。

### 2026-05-10 更新

#### 项目启动验证

**完成内容:**
- 已启动后端服务 `http://127.0.0.1:8000/`。
- 已启动前端服务 `http://127.0.0.1:5173/`。
- 已验证后端健康检查 `http://127.0.0.1:8000/health` 返回 `200 OK`。
- 已验证前端首页返回 `200 OK`。
- 已验证 `admin/admin123` 后端直连登录返回 `200 OK`。
- 已验证 `admin/admin123` 前端代理登录返回 `200 OK`。

### 2026-05-08 更新

#### 项目启动排查

**当前进展:**
- 前端开发服务 `http://127.0.0.1:5173/` 已在运行，并确认页面标题为“第二课堂活动通知系统”。
- 后端默认端口 `8000` 当前被另一个“桥隧病害智能检测可视化系统”服务占用，不是本项目后端。
- 本项目后端配置的健康检查路径应为 `/health`，但当前 `8000/health` 返回 `404`，确认端口冲突需要处理。
- 已按确认结束占用 `8000` 的桥隧项目进程。
- 已创建临时后端虚拟环境 `/private/tmp/2ketang-backend-venv` 并安装后端依赖。
- 已使用非热重载方式启动第二课堂后端服务: `http://127.0.0.1:8000/`。
- 已验证 `http://127.0.0.1:8000/health` 返回 `{"status":"healthy"}`。
- 2026-05-08 20:44 已按要求重新启动前后端：
  - 后端 `http://127.0.0.1:8000/` 正常返回“第二课堂活动通知系统API”。
  - 前端 `http://127.0.0.1:5173/` 正常返回 `200 OK`。
  - 前端代理 `/api/auth/me` 可转发到后端，未登录时正常返回 `Not authenticated`。

**待确认:**
- 当前前后端均已启动。后端使用临时 macOS 虚拟环境运行，非文档中的 Windows conda 环境。
- 登录接口访问数据库时出现 MySQL 权限错误：当前数据库账号被 `10.5.80.8` 拒绝访问，需要确认数据库账号、密码或授权范围。
- 2026-05-08 20:49 已复测登录链路：
  - `admin/admin123` 请求已到达后端，但数据库直连测试返回 `Access denied for user 'root'@'10.5.119.34'`。
  - 已将登录接口数据库异常从通用 `500` 改为明确 `503`，前端代理也能收到“数据库连接失败，请检查数据库配置、账号密码或远程访问权限”。
  - 已修正 `database_schema.sql` 中默认管理员 `admin/admin123` 的 bcrypt 哈希，并兼容修复早期错误哈希。
- 2026-05-08 21:48 已按本地数据库配置重新启动：
  - `.env` 已切换到本地 MySQL `127.0.0.1:3306`。
  - 后端重启后 `http://127.0.0.1:8000/health` 返回正常。
  - 本地数据库连接测试 `SELECT 1` 通过。
  - `admin/admin123` 后端直连登录返回 `200 OK`。
  - `http://127.0.0.1:5173/api/auth/login` 前端代理登录返回 `200 OK`。

### 2025-12-24 更新

#### 自动化通知脚本功能升级

**完成内容:**

1. **后端脚本任务管理**
   - 为3个通知脚本(新活动、签到、签退)添加任务管理支持
   - 脚本运行时创建任务记录，实时记录日志
   - 支持任务停止功能
   - 添加脚本任务类型枚举 (script_new_activity, script_sign_in, script_sign_out)

2. **脚本定时任务支持**
   - 为脚本添加定时任务API (/api/crawler/scripts/schedule/start)
   - 支持自定义运行间隔
   - 定时任务状态显示和倒计时

3. **前端界面改进**
   - 脚本卡片样式升级，与爬虫卡片保持一致
   - 添加启动/停止按钮
   - 添加最近运行时间显示
   - 添加定时任务开关和间隔设置
   - 添加倒计时显示

4. **日志实时刷新**
   - 日志对话框新增"自动刷新"复选框
   - 默认开启，每2秒自动刷新日志内容
   - 任务运行中日志实时更新

5. **Bug修复**
   - 修复脚本启动后状态显示"等待中"的问题（在API返回前设置运行状态）
   - 修复脚本无法正常执行的导入错误:
     - 添加 `crawl_activities` 函数到 `activity_crawler.py`
     - 添加 `login_and_get_driver` 别名到 `login.py`
     - 修复 `crawl_activity_participants` 函数调用参数错误
   - 修复签到签退数据获取问题:
     - 更新 Vue 数据字段匹配 (`code`, `name`, `inTime`, `outTime`)
     - 更新数据库保存逻辑支持新字段名
     - 支持多个签到/签退时间（逗号分隔，取第一个）
   - **修复签到/签退人数显示为0的bug**:
     - 原因: `save_participants_to_db` 中字段名不匹配
     - 实际字段名为 `inTime`/`outTime`，代码只检查了 `signInTime`/`signOutTime`
     - 已修复 `sign_in_notify.py` 和 `sign_out_notify.py`

**修改的文件:**
- `backend/app/models/crawler_log.py` - 添加脚本任务类型
- `backend/app/api/crawler.py` - 添加脚本任务管理和定时API
- `backend/app/scripts/new_activity_notify.py` - 添加日志记录支持
- `backend/app/scripts/sign_in_notify.py` - 添加日志记录支持，修复函数调用
- `backend/app/scripts/sign_out_notify.py` - 添加日志记录支持，修复函数调用
- `backend/app/crawlers/activity_crawler.py` - 添加 crawl_activities 函数
- `backend/app/crawlers/login.py` - 添加 login_and_get_driver 别名
- `backend/app/crawlers/participant_crawler.py` - 修复 Vue 数据字段匹配
- `backend/app/services/crawler_service.py` - 修复参与者数据保存字段映射
- `frontend/src/views/CrawlerManagement.vue` - 界面升级

### 2025-12-23 更新

#### 前端开发进度 (70%)

**已完成页面:**

| 页面 | 文件 | 功能 | 状态 |
|------|------|------|------|
| 登录页面 | `Login.vue` | 表单验证、JWT登录、路由跳转 | 已完成 |
| 数据看板 | `Dashboard.vue` | 统计卡片、活动状态分布、邮件统计、最近通知 | 已完成 |
| 活动列表 | `ActivityList.vue` | 活动列表展示、筛选 | 已完成 |
| 学生列表 | `StudentList.vue` | 学生信息展示 | 已完成 |
| 通知记录 | `NotificationLog.vue` | 通知历史记录 | 已完成 |
| 爬虫管理 | `CrawlerManagement.vue` | 爬虫任务控制 | 已完成 |
| 系统设置 | `Settings.vue` | 系统配置 | 基本完成 |
| 布局组件 | `Layout.vue`, `layout/index.vue` | 整体布局框架 | 已完成 |

**已完成配置:**

| 模块 | 文件 | 状态 |
|------|------|------|
| 路由配置 | `router/index.js` | 已完成（含路由守卫） |
| 状态管理 | `stores/auth.js`, `stores/user.js` | 已完成 |
| API封装 | `api/*.js` | 已完成（5个模块） |
| Axios配置 | `api/request.js` | 已完成 |

**待完善功能:**
- [ ] 活动详情弹窗/页面
- [ ] 学生邮箱编辑功能
- [ ] 爬虫日志实时查看
- [ ] 界面美化和响应式适配

---

### 2025-12-22 之前

#### 后端开发 (90%)

**已完成:**
- 认证API (JWT登录)
- 学生管理API (CRUD + 统计)
- 活动管理API (CRUD + 统计 + 筛选)
- 通知管理API (记录查询 + 邮件日志)
- 爬虫控制API (启动/停止/状态/日志)
- 邮件服务
- 学生匹配服务
- 通知服务
- 爬虫服务

#### 数据爬取 (100%)

**已完成:**
- 学生信息爬取: 30,376 条
- 活动列表爬取: 4,211 条
- 活动详情爬取: 4,200 条 (99.7%)

#### 数据库 (100%)

**已创建表:**
1. `students` - 学生表
2. `activities` - 活动列表表
3. `activity_details` - 活动详情表
4. `activity_notifications` - 通知记录表
5. `activity_participants` - 活动参与者表
6. `email_logs` - 邮件日志表
7. `users` - 管理员用户表
8. `crawler_logs` - 爬虫日志表

---

## 三、待开发功能

### 高优先级
1. **前端完善**
   - [ ] 活动详情页面
   - [ ] 邮箱批量导入功能

### 中优先级
2. **系统优化**
   - [ ] 数据库查询优化
   - [ ] 添加缓存机制

### 低优先级
3. **扩展功能**
   - [ ] 微信推送支持
   - [ ] 短信通知支持
   - [ ] 数据导出功能

---

## 四、已知问题

| 问题 | 状态 | 备注 |
|------|------|------|
| 测试邮箱只有1个 | 待处理 | 需要添加更多测试数据 |
| 活动详情页面缺失 | 待开发 | 路由已配置但复用了列表组件 |

---

## 五、启动说明

### 后端
```bash
cd backend
D:\Software_environment\env\2ketang\python.exe run.py
```
服务地址: http://localhost:8000

### 前端
```bash
cd frontend
npm run dev
```
服务地址: http://localhost:5173

---

## 六、变更日志

### 2025-12-24
- 升级自动化通知脚本功能
- 脚本添加任务管理和日志记录
- 脚本添加定时任务支持
- 日志对话框添加自动刷新功能
- 脚本卡片样式升级

### 2025-12-23
- 创建开发进度文档
- 同步前端实际开发状态

### 2025-12-22
- 完成主控爬虫
- 完成后端API开发
- 开始前端开发

### 2025-12-19
- 完成数据爬取模块
- 完成数据库设计
