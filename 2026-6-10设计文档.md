# 2026-6-10 需求 · 技术设计文档

> 基于平台真实 API 调研结果(见文末附录),把需求落成可实现的设计。
> 调研结论: **所有数据都能走源站 API 直调,无需 Selenium 翻页**,提速与老师识别均成立。

---

## 0. 核心结论

1. **爬虫重构为"生命周期 + 通知触发"模型**(对的方向):
   报名中 →(可报名通知)→ 待开始 →(报名成功通知)→ 进行中 →(签到/签退通知)→ 已完结
2. **全面 API 化**: 活动列表/报名列表/签到名单都是 `/manage/server` 接口,
   用学生爬虫已破解的认证(sessionKey + AES Authorization + cookie)直接调,秒级。
   Selenium 仅用于"登录拿 sessionKey/cookie",之后纯 HTTP。
3. **老师识别可行**: 报名/签到名单自带 `role` 字段(1=管理员/老师, 3=学生),
   配合 code 位数(老师4位工号 / 学生8位学号)双重确认。
4. **通道=微信订阅消息**(用户定): 有"每条消息需用户提前一次性授权"的硬约束,
   是本设计最大的产品约束,第 3 节专门设计应对。

---

## 1. 需求重述(生命周期模型)

| 阶段 | 平台 listType | 触发的通知 | 发给谁 | 对应爬虫 |
|---|---|---|---|---|
| 报名中 | 4 | "有新活动可报名" | 有参与权限的学生(按院系/年级匹配) | 爬虫3 |
| 待开始 | 5 | "报名成功" | 已报名的人(报名列表) | 爬虫4 |
| 进行中 | 6 | "该签到了" / "该签退了" | 报名列表里的人 | 爬虫5 |

外加两个数据底座:
- **爬虫1 学生信息**: 已完善,不动。
- **爬虫2 活动全量信息**: 手动,同步所有活动详情/QQ群/地点/时间。要求"学生爬虫的速度"→ API 化。

---

## 2. 关键技术发现(调研结论)

### 2.1 认证机制(全平台通用)
学生爬虫 `_source_api_get` 已破解,任何 `/manage/server/*` 接口都能用:
- 从浏览器 `localStorage.sessionKey` 取 token
- `Authorization` = AES-ECB(key=`xiangfubeitu2025`) 加密 `{"token","platform":3,"timestamp"}`
- 带浏览器 cookie
- 参数包成 `?params={json}` 的 query

### 2.2 接口清单(已验证可调)

| 用途 | 接口 | 关键参数 | 返回要点 |
|---|---|---|---|
| 活动列表 | `/activity/actAllList` | `listType`(4报名中/5待开始/6进行中) `current` `size` `statusAcitive` | actId/name/className/orgName/startTime/endTime/enrollEndTime/hours/adminId/adminCode/adminName |
| 报名列表 | `/activity/member-personal` | `id`(活动id) `current` `size` `status` `identity` | collegeName/**role**/code/identity/name/apply_time/userId/status |
| 签到签退 | `/activity/member/info/all` | `actId` `current` `size` `signIn`/`signOut`/`role`(筛选) | **role**/code/name/**singIn**/**singOut**/**inTime**/**outTime**/leave/userId |
| 活动详情 | (tab-1 无独立 XHR,字段在 actAllList 已够;QQ群/地点/院系需补查 actInfo,见待确认) | | |

### 2.3 老师 / 学生识别
报名列表 `member-personal` 实测(活动5309, 47人):
- `role` 分布: `{1:1, 2:1, 3:45}` → **role=1 活动管理员/老师, role=3 普通学生, role=2 待确认(疑似协办/助理)**
- `identity` 分布: `{2:1, 1:46}` → identity=2 那条正是 role=1 的管理员 → identity=2 教师 / identity=1 学生
- role=1 那条: `code=1018, userId=122119, name=建筑工程系团总支`,与活动的 `adminCode=1018/adminId=122119` 完全对应 → **就是活动负责老师本人**
- **老师 code 是 4 位工号(1018/1013),学生 code 是 8 位学号** → 位数可做兜底判据

识别方案: `role==1 或 identity==2` 即老师;精确定位活动负责老师 = 报名列表中 `userId==活动.adminId` 的那条。

### 2.4 签到 / 签退判断
签到名单 `member/info/all` 实测(进行中活动5280, 40人):
- **字段拼写坑**: 是 `singIn`/`singOut`/`inTime`/`outTime`(sing 少个 g),不是 signIn/signOut
- `inTime` 非空 = 已签到。实测 40 人中 32 人 inTime 有值 → 签到已开始
- `outTime` 非空 = 已签退。实测全空 → 还没签退
- 触发判据(需求): 已签到人数 > 阈值(需求写">2",建议做成可配置, 默认2)就发签到通知;签退同理

---

## 3. 通知通道: 微信订阅消息的约束与设计 ⚠️

用户选"只用微信订阅消息"。必须正视个人主体小程序的硬约束:

### 3.1 硬约束
- 个人主体**只能用"一次性订阅"**(`wx.requestSubscribeMessage`),拿不到"长期订阅"(仅政务/医疗等类目)。
- **一次授权 = 只能发一条**。学生点一次"允许",后端就只有一次发送额度。
- 想给学生发"签到提醒",该学生必须**事先**在小程序里授权过、且额度没用完。
- 微信不返回"剩余额度",只能自己记账 + 发送时靠错误码(43101=未授权)兜底。

### 3.2 设计: 订阅额度记账 + 触发消费
1. 小程序学生端: 在活动详情/通知设置页引导学生订阅(一次可请求多个模板, 勾"总是保持"减少弹窗)。
2. 后端建 `wechat_subscribe_quota` 表: `openid, 学号, template_id, 剩余次数`。每次 `requestSubscribeMessage` 成功授权 → 对应模板次数 +1(小程序回调后端登记)。
3. 爬虫触发通知 → 查该学号绑定的 openid + 模板剩余额度 > 0 → 调云端 wechat 代理 `/send` → 成功则额度 -1;失败(43101)则额度清零并标记"需重新订阅"。
4. 额度为 0 的学生收不到 → 小程序端用"小红点/提示"引导其再次订阅。

### 3.3 风险提示(写给决策者)
- 这套机制下,**通知到达率取决于学生有没有提前授权**,不像邮件能 100% 投递。
- 高频通知(每个活动报名+签到+签退=3条)意味着学生要频繁授权,体验有摩擦。
- 已申请的模板目前只有"签到提醒"(W4Os...)。报名成功/可报名/签退 **需在公众平台另申请模板**。
- **决定(已确认): 坚持纯微信,不加邮件兜底。** 接受"仅授权过的学生能收到"的代价,
  产品上靠小程序引导学生订阅来提升覆盖率。后端发送侧只实现微信通道。

---

## 4. 各爬虫详细设计

> 统一基于 `源站 API + 去重表`,Selenium 仅登录。

### 爬虫2 · 活动全量同步(手动)
- `actAllList` 遍历 listType=1..0 全部状态 + 分页 → 活动主表
- 详情字段(QQ群/地点/院系/年级): 补查活动详情接口(待确认 actInfo 接口名)或复用现有 detail_crawler 的页面提取
- QQ 群提取复用已修好的 `qq_extractor`
- 目标: 全量同步, 秒级(API)

### 爬虫3 · 报名中 → 可报名通知
- `actAllList listType=4` 取报名中活动 + 详情(院系/年级限制)
- `student_matcher` 按 college_name/grade_name 匹配有权限的学生("不限"→全部)
- 对每个匹配学生: 查 openid+额度 → 发"可报名"订阅消息
- 去重: `activity_notifications(act_id, type=enroll_open)` 每活动每学生只发一次

### 爬虫4 · 待开始 → 报名成功通知
- `actAllList listType=5` 取待开始活动
- 每活动调 `member-personal` 取报名列表(role=3 学生)
- 对每个报名学生发"报名成功"通知
- 去重: `(act_id, student_code, type=enroll_success)`

### 爬虫5 · 进行中 → 签到/签退通知(自动/手动)
- `actAllList listType=6` 取进行中活动
- 每活动调 `member/info/all`:
  - 签到爬虫: `inTime` 非空人数 > 阈值 → 给报名列表所有人发"该签到了"(每活动只发一次)
  - 签退爬虫: `outTime` 非空人数 > 阈值 → 发"该签退了"(每活动只发一次)
- 老师特殊处理(需求5.1): 报名列表中 role=1/identity=2 的老师, 用其 code 找"绑定管理员账号"的小程序用户, 发提醒
- 去重: `(act_id, type=sign_in/sign_out)` 活动级,只发一次
- 模式: 配置项 auto/manual,默认 auto
- 时效性: 进行中活动数量少(~16),API 调用快,可短间隔轮询(如每 2-5 分钟);需要时多协程并发(API 无需多开浏览器)

---

## 5. 老师/管理员绑定与权限(需求1.2 + 5.1)

- 小程序绑定页加两个按钮: **绑定本人学号** / **绑定管理员(老师)账号**,绑定后"确认绑定"→"解绑"
- 绑定管理员账号 = 绑定老师工号(4位)。后端 `openid ↔ 工号` 映射(管理员表)
- 爬虫5 发现活动负责老师(role=1)时,按其工号找绑定者发管理通知
- ⚠️ 管理端 UI 暂不设计,所以"绑定管理员"目前只用于**接收管理通知**,不开放管理操作

---

## 6. 学号验证系统(需求1.1, 后面额外设计)
- 当前绑定仅本地存储,无验证 → 任何人可绑任意学号
- 最小可行验证: 学号 + 姓名匹配学生表(已有数据) → 通过才绑定 openid
- 更强: 邮箱/短信验证码(学生表有 mobile 字段)
- 本文档先按"学号+姓名匹配"设计,验证系统细化另开

---

## 7. 模块划分与现有代码关系

```
crawlers/
  source_api.py    (新) 封装 _source_api_get/认证, 从 student_crawler 提取为公共
  activity_api.py  (新) actAllList / member-personal / member-info-all 的 API 封装
  login.py         (留) 登录拿 sessionKey/cookie
services/
  notify/          (新) 按生命周期的通知编排(enroll_open/success/sign_in/sign_out)
  student_matcher  (留) 院系/年级匹配
  wechat_service   (留) 订阅消息发送 + 额度
repositories/      (留, 阶段3已建) 各表存取
```
复用: qq_extractor / student_matcher / activity_notifications 去重 / repositories / wechat 代理

---

## 8. 数据库改动
- 新增 `wechat_subscribe_quota(openid, student_code, template_id, remaining, updated_at)`
- 新增 `admin_binding(openid, teacher_code, name, created_at)` (老师/管理员绑定)
- 复用 `activity_notifications` 扩展 notification_type 枚举: enroll_open / enroll_success / sign_in / sign_out
- `openid_student_map(openid, student_code, ...)` 学生绑定落库(目前仅本地)

---

## 9. 风险与待确认
1. **微信订阅覆盖率**(3.3): 纯订阅消息到达率受"学生是否提前授权"限制,是否加邮件兜底? **需确认**
2. **模板申请**: 除"签到提醒"外,可报名/报名成功/签退模板需在公众平台申请,字段需设计
3. **role=2 含义**待确认(疑似协办/助理),不影响主逻辑(只认 role=1 老师 / role=3 学生)
4. **活动详情字段接口**: QQ群/地点/院系/年级是否全在某 API,还是要保留页面提取,需补一次调研
5. **签到阈值**: ">2" 建议改可配置(默认2)
6. **并发时效性**: 进行中活动 API 轮询频率 + 是否并发,需定(API 化后并发成本低)

---

## 10. 建议实施顺序
1. 提取 `source_api` 公共模块 + `activity_api`(actAllList/member-personal/member-info-all 封装) — 地基
2. 爬虫2 活动全量 API 同步(替换现 selenium 活动爬虫) — 立竿见影提速,且不依赖通知
3. 数据库新表 + 学生绑定落库 + 学号验证(姓名匹配)
4. 微信订阅额度机制 + 模板申请
5. 爬虫3/4/5 通知编排(按生命周期)
6. 老师/管理员绑定与通知
7. 小程序绑定页改造(两个按钮 + 订阅引导)

---

## 附录: 调研原始发现(节选)
- 待开始活动 5309: 报名 47 人, role={1:1,3:45,2:1}, identity={1:46,2:1}
- 进行中活动 5280: 签到名单 40 人, 32 人 inTime 有值(已签到), outTime 全空
- 调研脚本: `backend/_legacy/discover_activity_api.py` / `discover_activity_api2.py`
